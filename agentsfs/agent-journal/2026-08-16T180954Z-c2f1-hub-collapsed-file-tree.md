---
description: Session — changed the Hub file tree to start collapsed and verified the Hub build.
---
## Learned / decided
- The Hub already had accessible folder disclosure, search reveal, and active-file reveal; the regression was the server template rendering every directory expanded.
- The default should be collapsed on workspace and file-view trees, while opening a file still reveals its ancestor path so the current note remains reachable.
## Open
- None for this change.
## Written directly
- Updated [[backlog/INDEX]] with the completed collapsed-file-tree item.
- Updated the Hub tree template and regression assertions.
