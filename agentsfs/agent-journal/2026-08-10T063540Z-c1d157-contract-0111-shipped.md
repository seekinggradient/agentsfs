---
description: Session — contract 0.11.1 shipped and deployed: the backlog spine SHOULD declare backlog@0.1 (making the directory a conforming backlog-workspace), final-token rule, path-qualified references; version machinery honored to the letter.
---
## Learned / decided
- The adoption review's conclusion held through implementation: 0.11.0's directory backlog and backlog-workspace@0.1 are one design, so adoption is three lines, not a redesign. 0.11.1 ships them: rule-13 SHOULD-adopt (the spine declaring markdownto: backlog@0.1 makes the directory a conforming workspace — validation, transactional verbs, live Hub boards; tool-less instances lose only the verbs), the final-token rule, path-qualified cross-file references.
- contracts.go's variants-incident comment is load-bearing process documentation: the released 0.11.0 AGENTS + spine texts were vendored byte-exact BEFORE the template moved, the current spine vendored as 0.11.1, buildinfo bumped, and the one test that assumed 0.11.0-is-current updated to assert existence+role for historical versions. Full go test/gofmt/vet green; deployed.
- Remaining for full adoption: per-instance migration of the two live backlogs (envelope line, clause-first swaps, six-space renormalization where present) — tracked in the ticket.
## Open
- Owner may want afs update guidance for local binaries still bundling 0.10.0 (doctor already says the right thing).
- Coordinate the migrations with the projection/pull work landing in this repo (time-disjoint, file-disjoint).
