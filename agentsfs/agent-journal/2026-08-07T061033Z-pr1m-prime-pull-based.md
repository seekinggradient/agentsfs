---
description: "Session — owner decisions: afs prime stays pull-based (contract instructs agents to run it; no session-start hooks), the role keeps the name backlog, and the contract's Orient-first now leads with prime while rule 13 gains the work-from-it clause."
---

# Prime is pull, not push; backlog keeps its name

## Learned / decided

- Owner (2026-08-07): **no session-start hook wiring for prime** — agents are instructed by the contract to run `afs prime` themselves. Orient-first in `AGENTS.md` now leads with it; rule 13 adds "work from it, not around it: … break a multi-step effort into tasks there before starting it." Recorded in [[backlog-and-tasks]] and [[harness-plugins]] decision logs; the hook-wiring task in [[backlog]] is dropped (`[-]`), and the harness-plugins decision's remaining scope is pre-compact capture + stop-time reminders.
- Naming reviewed and settled: **backlog** stays — the sprint-plan feel lives in the `Now` band and the contract's instruction, not the page name.
- Meta-instance `AGENTS.md` re-synced to the amended 0.10.0 template (was pristine stock; verified byte-identical to HEAD's template before overwrite).
- Doctor caught a malformed-frontmatter in the previous journal entry (unquoted colon in its description) — quoted; a nice live proof of the finding.

## Open

- Tracked in [[backlog]].

## Written directly

- `template/AGENTS.md` + meta-instance sync, both RFC decision logs, [[backlog]], `docs/agent-start.md`, `internal/core/prime.go` comment.
