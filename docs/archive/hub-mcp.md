---
description: Superseded — the Hub's remote MCP server is documented in docs/mcp.md, alongside the local afs mcp server.
---

# The Hub MCP server (superseded)

> **Superseded and unmaintained.** Read [../mcp.md](../mcp.md) instead, or run `afs docs mcp`.

This file was folded into [../mcp.md](../mcp.md), which covers **both** MCP servers — the local `afs mcp` over stdio and the Hub's remote `/mcp` over OAuth 2.1 — in one place, because the most common mistake is assuming they are the same server.
