name: ci

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Check out
        uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Test vendored MarkdownTo renderer
        run: node scripts/check-mdto-renderer.cjs

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Check formatting
        run: |
          files="$(gofmt -l $(find . -name '*.go' -not -path './dist/*'))"
          if [ -n "$files" ]; then
            echo "$files"
            exit 1
          fi

      - name: Check whitespace
        run: git diff --check

      - name: Test
        run: go test ./...
